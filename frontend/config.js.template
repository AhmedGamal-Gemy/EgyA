// Runtime configuration — generated by envsubst at container startup.
// Ports come from .env via docker-compose environment.
window.WHISPER_LIVEKIT_WS_URL = 'ws://localhost:${ASR_PORT}/asr';
window.STREAM_JUDGE_BASE_URL = 'http://localhost:${STREAM_JUDGE_PORT}';
