# Prompts — Report Agent

Placeholder. Prompt needed for `generate_report` (see `../tools/`).

This is the one place in the whole system where LLM-generated prose is the
right call (see plan section 6a's templated-vs-generated message
decision) — use it for `narrative_summary` specifically, while
`what_confused_students`/`wrong_answers`/etc. can stay closer to
structured extraction from the aggregated flags.
